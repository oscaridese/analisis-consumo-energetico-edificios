# Capturas

Guarda aquí la captura de pantalla del dashboard con el nombre exacto:

    dashboard.png

Después, en el `README.md`, busca el bloque de comentario que empieza por
`<!-- Cuando hayas guardado la captura...` y borra las dos líneas de comentario
(`<!-- ...` y `-->`) para que la imagen aparezca en el README.
